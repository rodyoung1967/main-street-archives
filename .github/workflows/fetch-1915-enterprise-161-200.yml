name: Fetch 1915 Enterprise pages 161-200

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/fetch-1915-enterprise-161-200.yml"

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Enterprise pages 161-200
        shell: bash
        run: |
          set -euo pipefail
          out="newspapers/oregon-city-enterprise/1915-pages-161-200"
          mkdir -p "$out"
          rm -f "$out"/*.pdf "$out/sha256.txt" "$out/manifest.json"
          export out
          fetch_one() {
            d="$1"; n="$2"; nn=$(printf '%02d' "$n")
            f="$out/${d}-seq-${nn}.pdf"
            url="https://oregonnews.uoregon.edu/lccn/sn00063700/${d}/ed-1/seq-${n}.pdf"
            curl --fail --location --retry 3 --retry-delay 1 --connect-timeout 20 --max-time 180 \
              --user-agent "Mozilla/5.0 (main-street-archives research)" -o "$f" "$url"
          }
          export -f fetch_one
          for d in 1915-05-21 1915-05-28 1915-06-04 1915-06-11 1915-06-18; do
            for n in $(seq 1 8); do
              bash -c 'fetch_one "$0" "$1"' "$d" "$n" &
              while [ "$(jobs -rp | wc -l)" -ge 8 ]; do wait -n; done
            done
          done
          wait
          test "$(find "$out" -maxdepth 1 -name '*.pdf' | wc -l)" -eq 40
          for f in "$out"/*.pdf; do
            test "$(head -c 4 "$f")" = "%PDF"
            sha256sum "$f" >> "$out/sha256.txt"
          done

      - name: Write retrieval manifest
        shell: python
        run: |
          import hashlib, json, pathlib
          from datetime import datetime, timezone
          out = pathlib.Path("newspapers/oregon-city-enterprise/1915-pages-161-200")
          dates=["1915-05-21","1915-05-28","1915-06-04","1915-06-11","1915-06-18"]
          items=[]
          global_page=161
          for date in dates:
              for seq in range(1,9):
                  pdf=out/f"{date}-seq-{seq:02d}.pdf"
                  items.append({
                      "chronological_page": global_page,
                      "file": pdf.name,
                      "date": date,
                      "sequence": seq,
                      "source_url": f"https://oregonnews.uoregon.edu/lccn/sn00063700/{date}/ed-1/seq-{seq}.pdf",
                      "sha256": hashlib.sha256(pdf.read_bytes()).hexdigest(),
                      "review_status": "PENDING VISUAL REVIEW"
                  })
                  global_page += 1
          manifest={
              "title":"Morning Enterprise 1915 chronological pages 161-200 archival scan manifest",
              "newspaper_lccn":"sn00063700",
              "retrieved_utc":datetime.now(timezone.utc).isoformat(),
              "page_count":len(items),
              "chronological_range":[161,200],
              "issue_dates":dates,
              "items":items
          }
          (out/"manifest.json").write_text(json.dumps(manifest,indent=2)+"\n")

      - name: Upload preserved PDFs
        uses: actions/upload-artifact@v4
        with:
          name: 1915-enterprise-pages-161-200-pdfs
          path: newspapers/oregon-city-enterprise/1915-pages-161-200
          retention-days: 7

      - name: Commit preserved PDFs and manifest
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add newspapers/oregon-city-enterprise/1915-pages-161-200
          git commit -m "Archive 1915 Enterprise pages 161-200"
          git pull --rebase origin main
          git push origin HEAD:main
